# Representacao computacional de um grafo por meio de matriz de adjacencias:
class MatrizAdjacencias:
    # inicializa o grafo:    
    def __init__(self, numVertices):
        print("Implementar construtor da classe MatrizAdjacencias\n")

    # retorna a ordem do grafo:
    def ordem(self):
        print("Metodo ordem(self) nao foi implementado ainda!\n")
        return None
    
    # retorna o tamanho do grafo:
    def tamanho(self):
        print("Metodo tamanho(self) nao foi implementado ainda!\n")
        return None

    # adiciona uma aresta (v1, v2) no grafo:
    # peso eh um parametro opcional
    def addAresta(self, v1, v2, peso = 1):
        print("Metodo addAresta(self, v1, v2, peso = 1) nao foi implementado ainda!\n")
        return None

    # retorna True se existe uma aresta (v1,v2) no grafo:
    def possuiAresta(self, v1, v2):
        print("Metodo possuiAresta(self, v1, v2) nao foi implementado ainda!\n")
        return None

    # retorna uma lista de tuplas (vertice, peso)
    # com os vizinhos de v:
    def vizinhos(self, v):
        print("Metodo vizinhos(self, v) nao foi implementado ainda!\n")
        return None
    
    # retorna o grau (saida) de um vertice:
    def grau(self, v):
        print("Metodo grau(self, v) nao foi implementado ainda!\n")
        return None

    # exibe o grafo no formato de matriz de adjacencias:
    def printGrafo(self):
        print("Metodo printGrafo(self) nao foi implementado ainda!\n")
        return None